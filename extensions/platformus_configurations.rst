Platformus.Configurations
=========================

This extension provides configuration classes (Configuration, Variable etc) with backend UI. Every other extension
can use Platformus.Configurations to work with user-defined configurations in unified way.